package handler

import (
	"net/http"
)

func (h *Handler) SignIn(w http.ResponseWriter, r *http.Request) {
	// role, resp := h.srv.GetType()
	// if role == usertypes.Client{
	// 	h.srv.Client.SignIn()
	// } else {
	// 	h.srv.Psychologist.SignUp()
	// }
}
